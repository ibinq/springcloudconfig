spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ibinq/springcloudconfig/
          search-paths: test
          username:
          password:
          default-label: master
server:
  port: 8888
